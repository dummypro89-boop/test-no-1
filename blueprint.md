# Lotto Number Generator

## Overview

This is a simple web application that generates 6 random numbers for a lottery ticket.

## Features

*   Generates 6 unique random numbers between 1 and 45.
*   Displays the generated numbers on the screen.
*   A button to generate new numbers.

## Current Task

### Plan

1.  **`index.html`**:
    *   Change the title to "Lotto Number Generator".
    *   Add a container for the generated numbers.
    *   Add a button to trigger the number generation.
2.  **`style.css`**:
    *   Add styles for the container and the generated numbers to make it look nice.
3.  **`main.js`**:
    *   Create a function to generate 6 unique random numbers between 1 and 45.
    *   Create a function to display the generated numbers in the container.
    *   Add an event listener to the button to call the number generation and display functions.
